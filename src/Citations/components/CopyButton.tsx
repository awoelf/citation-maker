import { Button } from '@nextui-org/react';

function CopyButton() {
    return (
        <Button auto className='copy-all-button'>
            Copy All
        </Button>
    );
}

export default CopyButton;
